# Build artifacts produced by make_bundle.sh / constructor — regenerated, not committed.
app_bundle.tar.gz
atomipy-*.sh
atomipy-*.pkg
atomipy-*.exe
# constructor's working/build cache
tmp/
*.tar.bz2
build/
cache/
