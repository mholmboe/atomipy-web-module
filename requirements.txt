flask
werkzeug
gunicorn
# atomipy is installed via git in the Dockerfile to get the latest version
# but if you prefer a PyPI version, just list 'atomipy' here.
# For now, we'll assume the Dockerfile handles the git installation.
numpy
scipy
pandas
matplotlib
tqdm
